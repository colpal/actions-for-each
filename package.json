{
  "scripts": {
    "watch": "npm run build -- --watch=forever",
    "build": "esbuild main.mjs --bundle --platform=node --target=node20 --outfile=dist/main.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "globby": "^14.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.8",
    "eslint": "^8.54.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.0"
  }
}
